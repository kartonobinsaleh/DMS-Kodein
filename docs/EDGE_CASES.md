# Edge Cases

## Borrow

- Cannot borrow if device is not AVAILABLE
- Cannot borrow if student not found
- Cannot borrow same device twice

## Return

- Cannot return if no active borrow record
- Cannot return already returned device

## System

- Handle empty data
- Handle network failure
- Handle invalid input

## Auth

- Prevent unauthorized access
- Handle expired session
